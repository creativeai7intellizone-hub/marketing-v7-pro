# Architecture
Static frontend → GitHub Pages → custom domain.
Content → JSON in `/data`.
Visuals → `/assets`.
Behavior → `/js`.
Styling → `/css`.
Automation hook → `/docs` + future n8n webhook integration.
Secrets → hosting/GitHub Secrets, never committed.
